<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Home</title>
    <style>
    h1:hover {
        transform: scale(1.05);
    }

    h1 {
        transition: transform 1s;
    }

    section {
        justify-content: center;
        align-items: center;
        color: white;
        background: linear-gradient(270deg, #ff6ec4, #7873f5, #4ade80);
        background-size: 600% 600%;
        animation: gradientMove 20s ease infinite;
    }

    @keyframes gradientMove {
        0% { background-position: 0% 50%; }
        50% { background-position: 100% 50%; }
        100% { background-position: 0% 50%; }
    }


    </style>
  {% include 'shared/navBar.jinja' %}
</head>
<body>
    <section class="bg-secondary text-white ">
        <div class="container">
            <div class="row align-items-center py-5" style="height: calc(100vh - 56px)">
                <h1 class="text-center display-1">
                       One Cart, Every Store
                </h1>
                <div class="text-center mx-auto" style="max-width: 600px">
                    Shop from multiple online stores and manage everything in one centralized cart. Compare prices, track orders
                </div>
                <div class="d-flex justify-content-center">
                    <a type="button" class="btn btn-primary p-3 fw-bold" href="/browse">Browse Products</a>
                </div>
            </div>
        </div>
    </section>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
