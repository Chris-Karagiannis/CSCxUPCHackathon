<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-rbsA2VBKQhggwzxH7pPCaAqO46MgnOM80zW1RWuH61DGLwZJEdK2Kadq2F9CUG65" crossorigin="anonymous">
    <link rel="stylesheet" href="{{ url_for('static', filename='css/browse.css') }}">
    <title>Browse</title>
    {% include 'shared/navBar.jinja' %}

    <style>
        .item-card {
            background: white;
            border-radius: 20px;
            overflow: hidden;
            box-shadow: 0 10px 30px rgba(0,0,0,0.1);
            transition: all 0.4s ease;
            margin-bottom: 2rem;
            margin-left: 10px;
            border: 3px solid transparent;
            text-align: center;
        }
    </style>

</head>
<body>
    <div>
        <div class="hero-section text-center mb-4">
            <h1>Browse Page</h1>
            <h2>Welcome to the Browse Page</h2>
            <p>Explore our collection of items.</p>
        </div>
        <form action="/search" method="GET">
            <div class="row mb-1 justify-content-center">
                <div class="col-lg-4 mb-3">
                    <div class="search-container">
                        <i class="search-icon"></i>
                        <div class="input-group">
                            <input type="text" class="form-control search-input" placeholder="Search for items...">
                            <button class="btn btn-primary search-button" id="searchButton">Search</button>
                        </div>
                    </div>
                </div>
            </div>
        </form>
        <div class="item-grid me-3 row g-3 pt-3">
            {% set items = [1,2,3,4] %}
            {% for i in items %}
                <div class="col-md-4">
                    <div class="item-card p-3">
                        <img src="#" alt="item" class="img-fluid mb-3" style="border-radius: 15px; max-height: 200px; object-fit: cover;">
                        <h5>Name</h5>
                        <p>Desc</p>
                        <a href="#" class="btn btn-primary">View Details</a>
                    </div>
                </div>
            {% endfor %}
        </div>
    </div>
</body>
</html>